{
  "name": "backend",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "start:dev": "prisma migrate dev && nest start --watch",
    "start:production": "prisma migrate deploy && nest start",
    "build": "nest build",
    "apigen": "prisma generate && nest start --config apigen.json"
  },
  "dependencies": {
    "@nestjs/common": "^8.4.4",
    "@nestjs/core": "^8.4.4",
    "@nestjs/platform-express": "^8.4.5",
    "@nestjs/swagger": "^5.2.1",
    "@prisma/client": "^4.1.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.13.2",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.5.5"
  },
  "devDependencies": {
    "@nestjs/cli": "^8.2.5",
    "@types/express": "^4.17.13",
    "@vegardit/prisma-generator-nestjs-dto": "^1.5.1",
    "prisma": "^4.0.0",
    "prisma-class-validator-generator": "^0.2.0"
  }
}
